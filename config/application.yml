server:
  host: 0.0.0.0
  port: 8080
  grpc_port: 50051
database:
  url: postgresql://root:password@localhost:5432/skeletondb
  max_connections: 10
  min_connections: 5
  connect_timeout: 30
  idle_timeout: 600
logging:
  level: info
  format: json
  file: logs/performance.log
module:
  name: performance
  enabled: true
features:
  soft_delete: true
  state_machines: false
  workflows: true
entities:
  appraisal:
    collection: appraisals
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
  appraisal_cycle:
    collection: appraisal_cycles
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
  feedback:
    collection: performance_feedback
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
  goal:
    collection: goals
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
  reward:
    collection: rewards
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
  talent_matrix_entry:
    collection: talent_matrix_entries
    enabled: true
    cache_ttl: 300
    pagination:
      default_limit: 20
      max_limit: 100
tracing:
  enabled: true
  exporter: otlp
  endpoint: http://localhost:4317
metrics:
  enabled: true
  endpoint: /metrics
  port: 9090
