//! TalentMatrixEntry REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::application::service::{ServiceError, TalentMatrixEntryService};
use crate::domain::entity::*;

// DTO imports
use crate::presentation::dto::{
    CreateTalentMatrixEntryDto, PatchTalentMatrixEntryDto, TalentMatrixEntryResponseDto,
    UpdateTalentMatrixEntryDto,
};

/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum TalentMatrixEntryError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for TalentMatrixEntryError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for TalentMatrixEntryError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "TALENTMATRIXENTRY_NOT_FOUND"),
            Self::Validation(_) => (
                StatusCode::BAD_REQUEST,
                "TALENTMATRIXENTRY_VALIDATION_ERROR",
            ),
            Self::Database(_) => (
                StatusCode::INTERNAL_SERVER_ERROR,
                "TALENTMATRIXENTRY_DATABASE_ERROR",
            ),
            Self::Internal(_) => (
                StatusCode::INTERNAL_SERVER_ERROR,
                "TALENTMATRIXENTRY_INTERNAL_ERROR",
            ),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for TalentMatrixEntry.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /talent_matrix_entries | List with pagination |
/// | POST | /talent_matrix_entries | Create new |
/// | GET | /talent_matrix_entries/:id | Get by ID |
/// | PUT | /talent_matrix_entries/:id | Full update |
/// | PATCH | /talent_matrix_entries/:id | Partial update |
/// | DELETE | /talent_matrix_entries/:id | Soft delete |
/// | POST | /talent_matrix_entries/bulk | Bulk create |
/// | POST | /talent_matrix_entries/upsert | Upsert |
/// | GET | /talent_matrix_entries/trash | List deleted |
/// | POST | /talent_matrix_entries/:id/restore | Restore |
/// | DELETE | /talent_matrix_entries/empty | Empty trash |
/// | GET | /talent_matrix_entries/:id/deleted | Get deleted by ID |
/// | DELETE | /talent_matrix_entries/trash/:id | Permanent delete from trash |
/// | GET | /talent_matrix_entries/count | Count active entities |
/// | GET | /talent_matrix_entries/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(TalentMatrixEntryService::with_repository(repository));
/// let router = create_talent_matrix_entry_routes(service);
/// ```
pub fn create_talent_matrix_entry_routes(service: Arc<TalentMatrixEntryService>) -> Router {
    BackboneCrudHandler::<
        TalentMatrixEntryService,
        TalentMatrixEntry,
        CreateTalentMatrixEntryDto,
        UpdateTalentMatrixEntryDto,
        TalentMatrixEntryResponseDto,
    >::routes(service, "/talent_matrix_entries")
}

/// Create Axum router with only the read (GET) endpoints for TalentMatrixEntry.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_talent_matrix_entry_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_talent_matrix_entry_read_routes(service: Arc<TalentMatrixEntryService>) -> Router {
    BackboneCrudHandler::<
        TalentMatrixEntryService,
        TalentMatrixEntry,
        CreateTalentMatrixEntryDto,
        UpdateTalentMatrixEntryDto,
        TalentMatrixEntryResponseDto,
    >::read_routes(service, "/talent_matrix_entries")
}

/// Create Axum router with only the write (mutation) endpoints for TalentMatrixEntry.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_talent_matrix_entry_write_routes(service: Arc<TalentMatrixEntryService>) -> Router {
    BackboneCrudHandler::<
        TalentMatrixEntryService,
        TalentMatrixEntry,
        CreateTalentMatrixEntryDto,
        UpdateTalentMatrixEntryDto,
        TalentMatrixEntryResponseDto,
    >::write_routes(service, "/talent_matrix_entries")
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_talent_matrix_entry_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<TalentMatrixEntryService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_talent_matrix_entry_routes(service).layer(middleware::from_fn(
        move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req
                    .headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| {
                        raw.strip_prefix("Bearer ")
                            .or_else(|| raw.strip_prefix("bearer "))
                    })
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => (
                        axum::http::StatusCode::UNAUTHORIZED,
                        axum::Json(serde_json::json!({
                            "success": false,
                            "error": "unauthorized",
                            "message": "Authentication required"
                        })),
                    )
                        .into_response(),
                }
            }
        },
    ))
}
